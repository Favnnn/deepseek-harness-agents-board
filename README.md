# deepseek-harness-agents-board
Agents board for the DeepSeek Harness web GUI: a draggable, resizable panel listing every agent and subagent across all chats. Cards show steps, context bars (green/yellow/red), tokens, LLM time and the preset; counters track running sessions, pending replies and total tokens. Settings toggle and language switch included; the window layout persists
